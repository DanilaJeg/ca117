#!/ust/bin/env python3

import sys
'''
def squash(line):
    counter = []
    i = 0
    while i < len(line):
        count = 1
        while i + 1 < len(line) and line[i] == line[i + 1]:
            i += 1
            count += 1
        counter.append(str(count) + line[i])
        i += 1
    return "".join(counter)

for line in sys.stdin:
    print(squash(line.strip()))

'''


